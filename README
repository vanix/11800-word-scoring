# 11800 電腦軟體應用丙級 · 文書處理評分系統

自動評分 Word (.docx) 作答檔案的 Flask 網頁應用，對應職類編號 11800 電腦軟體應用丙級。

## 功能

- 分階段評分：每個題組拆成多個階段，各自獨立定義檢查項
- 檢查項採 **JSON 參數驅動**（`scorer/exams/tz*.json`），隨時可調整不須改程式
- 檢查版面、邊界、頁首尾、字型、段落、取代、兩欄、圖片、表格等
- 扣分制評分，滿分 100，60 分及格
- 每字扣分（※）以 Levenshtein 編輯距離計算，支援「少打一個字扣 3 分」
- 上傳記錄自動寫入 `uploads/submissions.csv`

## 支援的題組與階段

| 題組 | 階段 |
|---|---|
| tz01 題組一 | 1 匯入並分段、2 邊界/頁首尾、3 標題/繕打、4 取代與格式、5 圖片/表格、6 表格內容與格式 |
| tz02 題組二 | 1 匯入並分段、2 頁首尾/字型/邊界、3 標題/繕打、4 取代與格式、5 插入圖片與表格 |

（舊版 tz03–tz05 已移除；其餘題組可依相同 JSON 結構擴充。）

## 各階段檢查內容

符號標記：**●**＝整項扣分（缺即扣滿分）｜**◎**＝全文比對（Levenshtein）｜**※**＝每字 3 分（錯字/漏字逐字扣）。

### 題組一（tz01）

#### 階段 1：匯入文字並分段
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 19 段 |
| 匯入文字與參考一致 | ◎20 | 全文與 `tz01_stage1_ref` 比對 |
| 正文段落左右對齊 | ●10 | 標題除外 |

#### 階段 2：邊界/頁首頁尾
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 19 段 |
| 匯入文字與參考一致 | ◎20 | 同階段 1 |
| 正文段落左右對齊 | ●10 | 標題除外 |
| 邊界 3 公分 | ●10 | 上/下/左/右皆 1701 twips |
| 頁首三欄 | ●10 | 學號／姓名／座號 |
| 頁尾三欄 | ●10 | 日期／第 X 頁 |
| 頁首字型 | ●10 | 新細明體／Times New Roman／10pt |
| 頁尾字型 | ●10 | 新細明體／Times New Roman／10pt |

#### 階段 3：輸入標題及繕打文字
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 21 段 |
| 正文段落左右對齊 | ●10 | 標題除外 |
| 邊界 3 公分 | ●10 | 沿用 |
| 頁首/頁尾（格式＋字型） | ●10×4 | 沿用階段 2 |
| 標題文字 | ※3/字 | 第 1 段，與 `tz01_stage3_ref` 比對 |
| 【】已刪除 | ※3/字 | 半型與全型括號是否已移除 |
| 繕打文字兩段 | ※3/字 | 第 7、9 段，每字比對 |

#### 階段 4：取代與格式設定
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 22 段 |
| 正文段落左右對齊 | ●10 | 標題除外 |
| 邊界／頁首／頁尾／字型 | ●10×6 | 沿用 |
| 標題文字、【】已刪除 | ※ | 沿用 |
| 取代 Amazon→亞瑪森（含底線） | ●10 | 比對取代結果與底線 |
| 半型 () 轉全型 （） | ●10 | 全文 |
| 標題 16pt／置中／框線／網底 | ●10 | 新細明體 32 half-pt |
| 第八九段兩欄（欄距 1cm） | ●10 | 欄距 567 twips |
| 各段字型與格式 | ●5 | 相對段落逐一檢查（見下） |

階段 4 各段格式（`para_spec`，相對段落序）：1 底線｜2 斜體＋首行縮排｜3 標楷體＋縮排｜4 標楷體＋縮排｜5 底線＋斜體｜6 標楷體＋斜體＋縮排｜7 縮排｜8 縮排｜9 標楷體＋斜體＋底線｜10 縮排＋框線＋網底。

#### 階段 5：圖片設定與插入表格
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 23 段 |
| 正文段落左右對齊、邊界、頁首尾、字型 | ● | 沿用 |
| 標題文字、【】、取代、全型括號、兩欄、標題格式 | ●/※ | 沿用 |
| 各段字型與格式 | ●5 | 同階段 4（相對段落，表格不影響） |
| 圖片 | ●10 | 文繞圖緊密／細框線（≤1pt）／尺寸 ±10%／水平=左邊界、垂直=段落頂端／綁定第 6 段 |
| 表格 6 列 4 欄 | ●10 | 僅結構 |
| 合併儲存格 | ●10 | 依 `tz01_stage5_ref` |

#### 階段 6：表格內容與格式設定
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 23 段 |
| 其餘文字／格式檢查 | ●/※ | 沿用階段 5 |
| 圖片、表格 6×4、合併儲存格 | ● | 沿用 |
| 表格文字與參考一致 | ※3/字 | 依 `tz01_stage6_ref` |
| 欄位寬度 | ●10 | 依參考 |
| 首列首欄網底 | ●10 | |
| 表格文字置中／分散對齊 | ●10 | 依參考 |
| 表格字型 | ●10 | 中=新細明體、英=Arial |
| 表格半型 () 轉全型 （） | ●10 | |

### 題組二（tz02）

#### 階段 1：匯入文字並分段
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 13 段 |
| 匯入文字與參考一致 | ◎20 | 依 `tz02_stage1_ref` |
| 正文段落左右對齊 | ●10 | 標題除外 |

#### 階段 2：頁首頁尾/字型/邊界
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 13 段 |
| 匯入文字 | ◎20 | 沿用 |
| 正文段落左右對齊 | ●10 | |
| 邊界 3 公分 | ●10 | 1701 twips |
| 頁首三欄 | ●10 | 中文西元年／空白／第 X 頁 |
| 頁尾三欄 | ●10 | 學號／姓名／座號 |
| 頁首字型 | ●10 | 新細明體／TNR／10pt |
| 頁尾字型 | ●10 | 新細明體／TNR／10pt |

#### 階段 3：輸入標題及繕打文字
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 13 段 |
| 左右對齊、邊界、頁首尾、字型 | ● | 沿用 |
| 標題文字 | ※3/字 | 第 1 段，依 `tz02_stage3_ref` |
| 【】已刪除 | ※3/字 | |
| 繕打文字 | ※3/字 | 第 7 段 |

#### 階段 4：取代與格式設定
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 14 段 |
| 左右對齊、邊界、頁首尾、字型 | ● | 沿用 |
| 標題文字、【】已刪除 | ※ | 沿用 |
| 半型 () 轉全型 （） | ●10 | |
| 半型 , 轉全型 ， | ●10 | |
| 標題 18pt／標楷體／置中／框線／斜體 | ●10 | 36 half-pt |
| 第四段兩欄（欄距 1cm） | ●10 | |
| 各段字型與格式 | ●5 | 相對段落（見下） |

階段 4 各段格式（`para_spec`）：1 底線｜2 首行縮排｜3 標楷體＋縮排｜4 縮排｜5 標楷體＋斜體＋底線＋**網底**｜6 縮排。

#### 階段 5：插入圖片與表格
| 檢查項 | 分 | 說明 |
|---|---|---|
| 段落總數 | ●10 | 15 段（於空白處插入表格） |
| 左右對齊、邊界、頁首尾、字型 | ● | 沿用 |
| 標題文字、【】、標題格式 | ●/※ | 沿用 |
| 各段字型與格式 | ●5 | 同階段 4 相對段落（含第 5 段網底） |
| 圖片 | ●10 | 文繞圖緊密／細框線（≤1pt）／**右下陰影**／尺寸 ±10%／水平=欄（左邊界）、垂直=段落頂端／綁定第 6 段 |
| 表格 4 列 4 欄 | ●10 | 僅結構 |
| 合併儲存格 | ●10 | 第 1 欄垂直合併（第 1~3 列）＋第 4 列第 3~4 欄合併 |

## 安裝與執行

```bash
pip install flask
python app.py
```

開啟瀏覽器 http://localhost:5000

macOS 上若 5000 埠被 ControlCenter 佔用，改用其他埠：

```bash
python3 -c "from app import app; app.run(debug=False, host='0.0.0.0', port=5001)"
```

如需區域網路連線，已預設綁定 `0.0.0.0`，其他電腦以 `http://你的IP:5000` 連線即可。

> **注意**：其他電腦無法連線時，請關閉 Windows 防火牆或在防火牆新增允許 **port 5000** 的輸入規則。

## 依賴

僅 Flask 為第三方套件，其餘皆使用 Python 標準函式庫：
`zipfile`、`xml.etree.ElementTree`、`re`、`json`、`csv`、`os`、`datetime`

相容 Python 3.14。

## 評分架構

- `stages`：題組的階段清單（id、名稱）
- `checks`：檢查項清單。每項含 `id`、`symbol`（●/◎/※）、`points`、`label`、`stages`（適用哪些階段）
- `by_stage`：同一檢查項可在不同階段覆寫參數（如段落數、每字扣分）
- 每項可指定 `ref_file`：指向該階段專用參考答案（`tzXX_stageN_ref.docx`）
- 引擎 `run(stage=N)` 只執行 `stages` 含 N 的檢查項，並合併 `by_stage` 參數

### 新增題組 / 階段

1. 在 `scorer/exams/` 建立 `tzXX.json`（仿照 tz01.json）
2. 把參考答案存為 `tzXX_stageN_ref.docx`
3. 如需要新檢查邏輯，在 `scorer/checks.py` 新增函式並註冊進 `REGISTRY`
4. 重啟伺服器即生效

## 評分慣例

- 中文「新細明體」、標楷體等字型以**使用者訂定的標準**為準；macOS 因字型取代存成「細明體」時不依參考檔（參考檔由 Word 產生時可能被換字型）
- 未指定的格式採「未指定則放行」：明確設錯才扣分
- 圖片尺寸慣用 ±10% 容差；表格欄寬可比對參考（可設容差）

## 專案結構

```
11800-word-scoring/
├── app.py              # Flask 主程式
├── scorer/
│   ├── engine.py       # 評分引擎（階段過濾、by_stage 合併）
│   ├── parser.py       # .docx 解析器（表格/圖片/頁首尾/版面）
│   ├── checks.py       # 檢查函式（41 項）+ REGISTRY
│   └── exams/          # 各題組 JSON 設定與各階段參考答案
├── templates/          # HTML 模板
├── static/             # CSS 樣式
├── data/               # 題本與術科檔案
└── uploads/            # 上傳檔案與評分記錄
```
